spring:
  application:
    name: licensingservice
  profiles:
    active:
      dev
  cloud:
    config:
      enabled: true
      uri: http://localhost:8800
#      discovery:
#        enabled: true
#        serviceId: configserver
